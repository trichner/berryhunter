apiVersion: v1
kind: Service
metadata:
  name: web-dev
spec:
  ports:
  - port: 80
  selector:
    app: web
    stage: dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
      stage: dev
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: web
        stage: dev
    spec:
      containers:
      - image: gcr.io/berryhunter-io/berryhunter-web:rev-210107f
        name: web
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 3
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 3