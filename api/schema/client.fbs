include "common.fbs";

namespace DeathioApi;

enum ActionType: ubyte {
    Hit = 0,
    Equip,
    Craft,
    Consume,
    Drop
}

table Action {
	item:Item;
	actionType:ActionType = Hit;
}

table Input {
  tick:ulong;
  movement:Vec2f;
  rotation:float;
  action:Action;
}

root_type Input;