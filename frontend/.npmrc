# See https://docs.npmjs.com/cli/v10/configuring-npm/npmrc
# With this setting, npm will stubbornly refuse to install (or even consider installing) any package that claims to not be compatible with the current Node.js version.
# engine-strict=true
